#include <stdio.h>

int main(){

    int idade;
    int quantidade;     //int = valores inteiros.
    float altura;       //float = numeros decimais.
    double peso;        //double = numeros mais precisos, mais casas decimais que o float.
    char letra;         //char = caracteres, l armazerar um array também, ou seja, a quantia de caracteres dentro das chaves.
iteralmente 1 caracter, sempre deve ter as aspas simples, apóstrofe.
    char nome[20];      //pode
    //se uma variavel não for inicializada ou um valor não for atribuido ela pode puxar algum valor que ja estava salvo na memoria, não necessariamente algo util ou correto.


}